export const AppConfig = {
    STORAGE_CONFIG: {
        TARGET_STORAGE: sessionStorage,
    },
    SNACKBAR_CONFIG: {
        HORIZONTAL: 'center',
        VERTICAL: 'top',
        TIMEOUT: 3000,
        ERROR_TIMEOUT: 60000,
    }
};
